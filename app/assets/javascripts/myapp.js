var ready;

ready = function() {

  $('#header').sidebar('show');

  // // section1 animations

  // var tl = new TimelineMax();

  // tl.to(".super_title", 3, {
    
  // })

  // tl.play();

}

$(document).ready(ready);
$(document).on('page:load', ready);
